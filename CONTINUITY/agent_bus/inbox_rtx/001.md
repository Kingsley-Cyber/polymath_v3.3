# 001 (REVISED) — make ONNX boot-persistent, refresh the control, start the bus

The Mac session already received your HTML report from :8091 — good work; the
CUDA-13-nightly finding is recorded in the project docs. Promotion happened a
different way than originally planned: the app's extraction-engine settings now
point production at your **8086 instance directly**. **DO NOT swap ports.** 8086
must stay exactly as it is (ONNX fp32, FORWARD=32, batch 256/256/512).

Do, in order:

1. `git pull` — must reach `31dab9b` or later (PROTOCOL.md and this file arrive
   with it).
2. **Boot persistence**: update/create the Windows Scheduled Task so that on boot,
   port **8086** relaunches with its current ONNX setup (`.venv_onnx`,
   `GHOST_B_GLINER_ONNX=1`,
   `GHOST_B_GLINER_ONNX_REPO=E:\Polymath_Training\gliner_onnx_medium_v2.1`,
   `GHOST_B_GLINER_ONNX_DEVICE=cuda`, `GHOST_B_GLINER_FORWARD=32`,
   `GHOST_B_GLINER_BATCH=256 GHOST_B_FACET_BATCH=256 GHOST_B_GLIREL_BATCH=512`).
   Production now depends on 8086 surviving a reboot. Don't restart it if it's
   already healthy — just make boot bring it back.
3. **Control refresh**: relaunch **8084** from the pulled code as the torch-CUDA
   control (`.venv_sidecar`, NO ONNX envs, same batch envs). After `322cc4f` its
   GLiNER must land on CUDA — verify `/health` shows `gliner.device=="cuda"`
   (if `cpu`, the restart didn't pick up the pull). The Mac benches it remotely
   for the record.
4. **Outbox migration**: the bus wants port 8091 serving `E:\polymath_v3.3\agent_outbox`.
   Stop the current report server on 8091, create `agent_outbox`, MOVE the report
   html + raw artifact JSONs into it (they stay fetchable), then run detached:
   `python -m http.server 8091 --directory E:\polymath_v3.3\agent_outbox --bind 0.0.0.0`
5. Write `001_reply.md` per PROTOCOL.md: confirmation of each step, the Scheduled
   Task definition you installed, and both `/health` `gliner` blocks.
6. **Push test**: branch `rtx/push-test`, one commit adding
   `CONTINUITY/agent_bus/rtx_push_test.txt` containing `ok`, push once, report the
   exact result (success or error). Return to `main` either way.
7. Set `latest.txt` to `001_reply.md`, start the heartbeat, and run the PROTOCOL
   loop from now on.
