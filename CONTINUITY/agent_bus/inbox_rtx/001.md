# 001 — role-swap the sidecars, deliver the pending report, test push

Context from the Mac session: it benched both your instances with 256 real corpus
chunks — ONNX `:8086` = **19 ms/chunk**, torch `:8084` = **253 ms/chunk** (its
GLiNER was silently CPU-placed; fixed in commit `322cc4f`, which your pull brings).
ONNX is promoted to production. Output equivalence is confirmed (single entity diff
across 256 chunks, fp-boundary at the 0.45 threshold).

Do, in order:

1. `git pull` — must reach `7153a1b` or later.
2. Relaunch **8084 with the ONNX envs currently on 8086**, from `.venv_onnx`:
   `GHOST_B_GLINER_ONNX=1`,
   `GHOST_B_GLINER_ONNX_REPO=E:\Polymath_Training\gliner_onnx_medium_v2.1`,
   `GHOST_B_GLINER_ONNX_DEVICE=cuda`, `GLIREL_CKPT_DIR` as before,
   `GHOST_B_GLINER_BATCH=256 GHOST_B_FACET_BATCH=256 GHOST_B_GLIREL_BATCH=512`.
   Update the Scheduled Task so 8084 relaunches as ONNX on boot from now on.
3. Relaunch **8086 with plain torch envs** (NO `GHOST_B_GLINER_ONNX` vars), same
   batch envs, from `.venv_sidecar`. After the pull its GLiNER lands on CUDA — this
   instance is the torch-CUDA control.
4. Verify locally: `http://localhost:8084/health` must show `gliner.backend=="onnx"`
   with `CUDAExecutionProvider` in providers; `:8086/health` must show
   `backend=="torch"` with `gliner.device=="cuda"` (if it says `cpu`, the
   pull/restart didn't take — fix before replying).
5. Write `001_reply.md` containing:
   - both `/health` JSONs,
   - pip versions of `onnxruntime-gpu` and `torch` in `.venv_onnx`, and which
     install rung worked (plain PyPI / cu13 index / nightly / DirectML),
   - the full content of your earlier `RTX_ONNX_REPORT.md` if it exists on disk
     (it never reached the Mac), else say it doesn't exist,
   - peak nvidia-smi GPU utilization you observed during any ONNX run.
6. Push test: create branch `rtx/push-test` with one trivial commit (add file
   `CONTINUITY/agent_bus/rtx_push_test.txt` containing `ok`) and `git push -u origin
   rtx/push-test`. Report success or the exact rejection error in `001_reply.md`.
   One attempt only; return to `main` afterwards either way.
7. Update `latest.txt` to `001_reply.md`, then resume the loop per PROTOCOL.md.
