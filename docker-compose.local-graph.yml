# Legacy no-op overlay.
#
# The old optional extraction overlay installed extra model packages into the backend.
# Ingestion now uses local vLLM LFM/Gemma pools instead, so this overlay
# intentionally adds no services or build args. It remains as a harmless
# compatibility file for existing compose commands that still include it.
services: {}
