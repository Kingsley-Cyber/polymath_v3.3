# Graphify Quality Fixture

This fixed document exercises entity quality, exact offsets, predicate direction, assertion qualification, open relations, ambiguous names, and pathological coordination.

## Core Systems and Canonical Predicates

Polymath uses MongoDB to store canonical evidence.

Polymath depends on Qdrant for semantic retrieval.

Qdrant supports vector search.

Neo4j is part of the Graphify projection layer.

The ingestion worker produces RelationAssertion records.

The graph writer consumes validated assertion batches.

Northstar Labs owns the Atlas Dataset.

Malformed aliases cause duplicate nodes.

The derived index is derived from the canonical document store.

The Architecture Guide defines the rebuildability contract.

Graphify implements the Projection API.

The Evidence Ledger is related to the Run Ledger.

## Libraries, Services, Datasets, and Processes

Graphify uses Pydantic for contract validation.

Graphify uses spaCy for dependency parsing.

The ingestion service depends on Redis for transient coordination.

The API Gateway supports the Query Service.

The Query Service consumes the Search Dataset.

The Report Builder produces the Evaluation Report.

The Projection Worker implements the Graph Write Process.

The Graph Write Process is part of the ingestion workflow.

## Passive Voice and Direction

The Projection API is implemented by Graphify.

Validated assertion batches are consumed by the graph writer.

The Atlas Dataset is owned by Northstar Labs.

Rebuildability is defined by the Architecture Guide.

Semantic retrieval is supported by Qdrant.

The query planner is implemented by the Query Service.

## Definitions, Aliases, and Lowercase Names

The Architecture Guide defines recoverability as the ability to reconstruct projections from canonical evidence.

Retrieval-Augmented Generation (RAG) is a named retrieval concept.

RAG depends on vector search.

Neo 4J, also written Neo4j, supports graph traversal.

The Service Level Objective (SLO) defines the P95 Latency target.

Pydantic is a named Python library for data validation.

pandas is a named Python library used for tabular analysis.

redis is a named software product used for transient coordination.

## Coordination

Graphify uses Pydantic, spaCy, and pandas.

MongoDB and Qdrant support the ingestion workflow.

The Query Service consumes the Search Dataset and the Audit Dataset.

Northstar Labs owns the Atlas Dataset and the Recovery Dataset.

The Report Builder produces the Evaluation Report and the Recovery Report.

Qdrant supports vector search and hybrid search.

## Negation, Modality, Conditions, and Attribution

Polymath does not depend on SQLite for canonical evidence.

Graphify may support PostgreSQL.

Maya Chen claims that Qdrant causes no data loss.

The Evaluation Report suggests that MongoDB supports temporal queries.

If Redis fails, the ingestion service may depend on the Local Queue.

Northstar Labs denied that it owns the Phantom Dataset.

The Architecture Guide recommends that Graphify use Pydantic.

The August Migration might cause temporary latency.

## Generic Noun and Pronoun Traps

The successor was discussed in the directive.

This approach uses a process.

The owner owns the end goal.

Taylor & Francis produces information.

The system supports the component.

The version depends on the process.

It uses MongoDB.

They support Qdrant.

A directive causes a change.

The method implements the approach.

## Ambiguous Surface Forms

Go is a named programming language used by the Graphify CLI.

Operators go to the control room in El Paso.

Apple owns the Orion Lab.

The apple caused a stain on the table.

Python implements the parser prototype.

A python crossed the road in El Paso.

Rust supports the Command Line Service.

The rust caused a surface defect.

Oracle supports the Billing Service.

The oracle predicted an event.

## Metrics, Locations, and Events

The Evaluation Report defines P95 Latency as 120 milliseconds.

The August Migration occurred in El Paso.

The August Migration caused temporary latency.

The Recovery Test Event produced the Recovery Report.

The Audit Process consumes the Recovery Dataset.

The Deployment Process uses the Identity Service.

The Identity Service depends on the Projection API.

The Projection API supports the Deployment Process.

## Open Relations

Maya Chen served in El Paso.

Northstar Labs published the Atlas Dataset.

The Recovery Report was authored by Maya Chen.

Graphify interoperates with the Identity Service.

The August Migration occurred after the Recovery Test Event.

The Atlas Dataset was curated by Northstar Labs.

## Complex Frames and Tail Cases

Graphify's implementation of the Projection API depends on Pydantic.

The Audit Worker that consumes the Audit Dataset produces the Evaluation Report.

Qdrant, which supports vector search, depends on the Identity Service.

The Report Builder, a named software tool, produces reports.

The Rebuild Process, which uses MongoDB, produces Neo4j projections.

The Audit Service was built on top of Redis.

The Search Service is a component of the Query Platform.

MongoDB, Qdrant, Neo4j, Redis, and PostgreSQL support the Data Platform, the Query Platform, and the Audit Platform.
