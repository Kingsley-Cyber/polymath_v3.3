# Optional local GraphRAG extraction lane.
#
# This file is intentionally not installed by the default backend Dockerfile.
# Install it only on machines that should run local GPU extraction:
#   pip install -r requirements.local-graph.txt
#
# Keep these pins conservative. Unpinned GLiNER currently pulls newer
# transformers / huggingface-hub / click / typing-extensions versions that
# conflict with the parser stack in requirements.txt.
gliner==0.2.17
gliner2
transformers==4.51.3
huggingface-hub==0.36.2
torch==2.3.1
click==8.1.7
typing-extensions==4.9.0
