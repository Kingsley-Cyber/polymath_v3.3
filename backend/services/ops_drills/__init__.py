"""Operational drill instrumentation (crash-injection seams).

Nothing in this package alters extraction semantics; every seam is inert
unless explicitly armed through environment variables by an operator.
"""
