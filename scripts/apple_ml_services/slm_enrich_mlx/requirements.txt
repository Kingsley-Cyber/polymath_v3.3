# Pass-2 enrichment sidecar (Apple Silicon).
fastapi
uvicorn
pydantic>=2

# --- MLX backend (default; prompt-only) ---
mlx-lm>=0.19
# model: an existing MLX build, no convert needed, e.g.
#   APPLE_SLM_ENRICH_MODEL_ID=Unravler/LFM2-1.2B-Extract-MLX-4bit

# --- GGUF backend (SLM_ENRICH_BACKEND=gguf; prompt + HARD JSON-schema grammar) ---
# Install with Metal:  CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
llama-cpp-python>=0.3.2
# model: LiquidAI/LFM2-1.2B-Extract-GGUF  ->  APPLE_SLM_ENRICH_GGUF_PATH=/path/to/*.gguf
