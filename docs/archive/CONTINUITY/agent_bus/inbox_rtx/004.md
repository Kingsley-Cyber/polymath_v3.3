# 004 — quick GPU telemetry snapshot (no changes)

Read-only request, reply fast: the human wants current RTX load during the
backfill (the Mac routes all extraction to your :8084 torch instance in
bursts).

1. Run `nvidia-smi` once and capture: GPU utilization %, memory used / total,
   and the per-process memory list.
2. Run it twice more ~20s apart so we catch at least one extraction burst if
   one lands.
3. Write `004_reply.md` with the three snapshots verbatim + one line noting
   which processes are the sidecars (8084 torch / 8086 onnx).
4. Update `latest.txt`, resume the loop. Nothing else — do not restart or
   reconfigure anything.
