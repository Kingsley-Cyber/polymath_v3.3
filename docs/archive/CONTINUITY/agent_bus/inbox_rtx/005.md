# 005 — URGENT, one action: stop the idle 8086 ONNX process to free VRAM

GPU is ~93/96 GB allocated: 8084 torch sidecar at ~71.8 GB (caching-allocator
high-water from the monster books — fine, it reuses that pool) + 8086 ONNX at
~21.5 GB while serving ZERO traffic (it's disabled in app settings since the
CUBLAS incident). If a remaining doc needs a new high-water mark on 8084
there is no headroom → CUDA OOM repeat.

1. STOP the 8086 sidecar process (pid ~17328) — kill the process only. Leave
   the Scheduled Task installed (it returns on next logon; we soak-test it
   properly after the backfill).
2. DO NOT touch 8084 (pid ~22072) in any way — it is carrying the live run.
3. `nvidia-smi` after: confirm 8086's memory released and note the new
   used/total.
4. Write `005_reply.md` with the before/after numbers (fold in the 004
   snapshots if not yet delivered), update `latest.txt`, resume the loop.
