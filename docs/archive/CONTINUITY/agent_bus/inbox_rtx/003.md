# 003 — URGENT: 8086 ONNX instance is failing under load (CUBLAS alloc); stabilize it

Context: the 498-file backfill is running. ~193 docs failed extraction with
HTTP 500 from :8086 — reproduced from the Mac:
`CUBLAS failure 3: the resource allocation failed ; GPU=0` (ORT CUDA EP,
cuda_call.cc:122). Diagnosis: torch GLiREL VRAM spikes on relation-dense
books (you measured 37 GB peaks) + ORT's CUDA arena + concurrent /extract
from 3 documents ⇒ allocation failure. Production has ALREADY been flipped
to your torch instance :8084 via app settings — the backfill is healing.
8086 receives no production traffic until this is fixed.

Do:

1. Tail `E:\polymath_v3.3\logs\ghost_b_extract_svc_8086.log` — paste the
   last 2-3 full ONNX/CUDA tracebacks into your reply (confirm they match
   CUBLAS alloc failures, flag anything different).
2. Note GPU memory state: `nvidia-smi` snapshot now (the backfill keeps 8084
   busy — that's expected traffic).
3. Restart 8086 with a memory-safer config (same launcher, new env values):
   `GHOST_B_GLIREL_BATCH=128` (was 512 — the main VRAM spike lever),
   `GHOST_B_GLINER_BATCH=128`, `GHOST_B_FACET_BATCH=128`, keep
   `GHOST_B_GLINER_FORWARD=32`. Update the Scheduled Task
   PolymathGhostBSidecarONNX to these values too.
4. Self-test: POST a LARGE payload at 8086 several times concurrently (3
   parallel POSTs of ~64 dense chunks each — reuse texts from
   local_ghost_b/onnx_equivalence_check.py _FALLBACK_TEXTS ×8) and confirm
   no 500s and stable VRAM.
5. Write `003_reply.md` (tracebacks, nvidia-smi, new config, self-test
   result), update `latest.txt`, resume the loop. The Mac will re-promote
   8086 after the backfill completes and a soak test passes — do NOT change
   8084 in any way; it is carrying production right now.
