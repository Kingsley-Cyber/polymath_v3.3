# 002 — get the Windows launchers + boot tasks into source control

Your 001 work is verified from the Mac side (both /health blocks match live state,
`origin/rtx/push-test` confirmed) and the torch-CUDA control is benched: 25 ms/chunk
on the Mac's 256-real-chunk payload vs 19 for your ONNX instance — production stays
:8086. Good work on the identity fix and the loud push flag.

The gap this exposes: `run_sidecar_windows.ps1`, `run_sidecar_onnx.ps1`, and both
Scheduled Task definitions exist ONLY on your disk — the production extraction
setup is not reconstructible from the repo. Fix via a branch (your no-push-to-main
boundary stands; the Mac merges):

1. Create branch `rtx/launchers` from current `main`.
2. Add to `scripts/apple_ml_services/`:
   - `run_sidecar_windows.ps1` (torch launcher, as deployed for 8084)
   - `run_sidecar_onnx.ps1` (ONNX launcher, as deployed for 8086)
   - `windows_tasks/PolymathGhostBSidecar.xml` and
     `windows_tasks/PolymathGhostBSidecarONNX.xml` — export via
     `schtasks /Query /TN <name> /XML`. SCRUB anything sensitive (machine user
     name is fine to keep; no passwords should appear in user-level tasks).
   - `windows_tasks/README.md` — 5 lines: how to import the XMLs on a fresh box
     (`schtasks /Create /TN <name> /XML <file>`) + the venv each task expects.
3. Commit on the branch (repo-local identity you configured), push once, report
   the exact result.
4. Write `002_reply.md` (include the branch head SHA + file list), update
   `latest.txt`, resume the loop.
