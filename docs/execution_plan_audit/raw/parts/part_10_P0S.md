### P0 Summary Integrity (frozen lines 347-401, chunk P0S)

| ID | Claim (verbatim) | Status | Disposition | Auditable action | Who | Where | When | Why | Feasibility | Required receipt |
|---|---|---|---|---|---|---|---|---|---|---|
| P0S:L351 | [x] Exclude explicit empty-model summary points from Funnel A. | x | done-unverified | Re-verify Funnel A empty-model exclusion via code probe: grep the exclusion filter in backend/services/retriever/funnel_a.py and confirm the summary-point query path filters/never surfaces summary_model=='' points; then run one Funnel A retrieval against a corpus and assert 0 returned points with empty summary_model. | junior-agent (read-only code+query probe) | backend/services/retriever/funnel_a.py; Qdrant corpus_{cid8}_naive/hrag summary points (payload summary_model) | LOG | Placeholder summaries in the breadth funnel poison recall with non-abstractive text presented as summaries. | already-done — No receipt on the ledger line, but the condition is now vacuously enforced: plan-critique V receipt (PLAN_CRITIQUE_2026-07-13.md sec.2) shows 0 empty-model summaries remain on any corpus, and registry_leftover 'empty summary_model' cites the write-side guard (qdrant_writer.py:847-866) preventing re-entry. Terminology: 'Funnel A' summary breadth means parent-level chunk_type='summary' points (registry_leftover 'summary points'), NOT Tier-0 doc_summary cards (registry_hierarchy 'doc summary' collision). | Grep hit for the empty-model filter in funnel_a.py + one live Funnel A probe returning 0 points with summary_model==''; restamp the ledger line with the probe date (Standing Rule 3). |
| P0S:L352 | [x] Refuse new summary writes without explicit model provenance. | x | done-unverified | Re-verify the write refusal: attempt a negative-control upsert_summaries call (test env) with summary_model='' and assert the point is skipped; confirm the guard at backend/services/storage/qdrant_writer.py:847-866 is present in the deployed image. | junior-agent (asserting test probe, non-destructive) | backend/services/storage/qdrant_writer.py:847-866 (upsert_summaries); Qdrant per-corpus collections | LOG | Prevention half of P0.1 — keeps the placeholder class from regenerating after cleanup. | already-done — registry_leftover 'empty summary_model' row cites the exact guard lines ('unmodeled summary placeholder skip at write', qdrant_writer.py:847-866) — code exists and is the canonical prevention seam. No dated receipt on the ledger line; a one-shot negative-control assert closes it per the assert-before-commit discipline. | Asserting test (non-zero exit on fail) that a summary_model=='' write is refused/skipped; run green inside the deployed backend container. |
| P0S:L353 | [x] Count only summaries actually accepted by the Qdrant writer. | x | done-unverified | Re-verify accounting honesty: confirm the summary-count metrics are derived from Qdrant writer acceptance (point_label='summary' upsert results, qdrant_writer.py:954-958) rather than from generation-side counts; cross-check p0_1_summary_integrity.py verify totals against a direct Qdrant count for one corpus. | junior-agent (read-only probe) | backend/services/storage/qdrant_writer.py:954-958; backend/scripts/p0_1_summary_integrity.py; Qdrant corpus_{cid8}_naive/hrag | LOG | Silent-fallback accounting rule — counting generated-but-rejected summaries would fake coverage numbers used by every later gate. | already-done — No receipt on the line, but the verify harness that PASSES all four corpora (PLAN_CRITIQUE sec.2, V) counts via the writer-side metric (empty_model_points at p0_1_summary_integrity.py:516 per registry_leftover). Consistency between script totals and a raw Qdrant scroll count is the remaining spot-check. | For one corpus: `p0_1_summary_integrity.py verify` reported summary total == direct Qdrant count of chunk_type='summary' points (exact match). |
| P0S:L354 | [x] Reconcile the 500 queued summary jobs against current artifacts. (2026-07-13: all 500 belonged to the deleted authentic_library; superseded with reason cor… | x | done-verified | Accept receipt commit 1171e9d — verified 2026-07-13. Optional re-check: Mongo summary_jobs count {status:'queued'} == 0 and count {superseded_reason:'corpus_not_active_orphan_job'} == 500; confirm the backup artifact exists. | script (one-time reconciliation, already executed); junior-agent for the re-check | Mongo summary_jobs collection; backup artifact from the 1171e9d run | LOG | Orphan queued jobs against a deleted corpus would fire paid regeneration into a void and corrupt job accounting. | already-done — Receipt is a commit (1171e9d) with reason-coded supersede + backup, matching the durable-baseline '500 queued' line it retires. Consistent with the checklist baseline (authentic_library deleted/quarantined). Nothing contradicts it in the plan critique. | Commit 1171e9d in history + Mongo probe: queued==0, superseded with reason corpus_not_active_orphan_job==500, backup file present. |
| P0S:L355 | all 500 belonged to the deleted authentic_library; superseded with reason corpus_not_active_orphan_job, backed up, 0 queued remain — commit 1171e9d. | na | done-verified | Re-check the numbers: Mongo summary_jobs — assert 0 docs with status 'queued'; assert 500 docs carry superseded_reason 'corpus_not_active_orphan_job' and all reference authentic_library's corpus_id; verify the backup file row-count == 500. | junior-agent (read-only Mongo probe) | Mongo summary_jobs; backup artifact of the supersede run | LOG | The zero-queued state is the precondition for trusting every later summary-jobs number in the baseline. | already-done — Fact-claim duplicating L354's receipt (commit 1171e9d). Internally consistent: baseline said 500 queued, authentic_library is the deleted corpus, reason code is specific and greppable. No colliding terminology. | Mongo aggregate output: {queued:0, corpus_not_active_orphan_job:500, all corpus_id==authentic_library} + backup row-count 500. |
| P0S:L358 | [x] Backfill valid summaries for every summary-required parent. (2026-07-13 restamp: p0_1_summary_integrity.py verify PASSES all four corpora ... NOTE (plan cr… | x | done-verified | Accept receipt (2026-07-13 restamp; plan-critique V confirms verify PASS on all four corpora, exit 0). Do NOT re-run paid backfill. Track the acknowledged residue separately: pair rows are back-level on capture fields (latent_concepts/temporal_class) and the S4 consolidated pass carries them — re-verify field completeness only AFTER S4. | script (p0_1_summary_integrity.py, executed); backend ingest worker for the S4 follow-up | backend/scripts/p0_1_summary_integrity.py; Mongo parent_chunks; Qdrant summary points (all four corpora) | LOG | 100% attributed summary coverage is the floor for Funnel A breadth and for every downstream card/lexicon build. | already-done — Double-receipted: ledger restamp + independent auditor V (PLAN_CRITIQUE sec.2: 'P0.1 verify: PASS on all four corpora, exit 0, host run today'). The C1 caveat is honestly recorded on the line itself — this is the canonical example of Standing Rule 1's violation, and the fix (S4) is correctly NOT re-opening this box. The two stale IN PROGRESS tags the critique flagged (C4) were the adjacent hygiene issue, resolved per Rule 3. | Already held: `p0_1_summary_integrity.py verify` exit 0 all four corpora, 2026-07-13. Post-S4 addendum: same verify green PLUS latent_concepts/temporal_class coverage counts on the pair > 0. |
| P0S:L359 | p0_1_summary_integrity.py verify PASSES all four corpora — polymath_v2 86,880 + ecommerce 9,453 + markbuilds 1,009 + UGO 203 all attributed, 0 empty-model; pas… | na | done-verified | Re-check the numbers by re-running `python backend/scripts/p0_1_summary_integrity.py verify` (host or container) and diffing per-corpus totals against 86,880 / 9,453 / 1,009 / 203 all-attributed, 0 empty-model. | junior-agent (script re-run, read-only) | backend/scripts/p0_1_summary_integrity.py; Qdrant corpus collections for polymath_v2, ecommerce_AI_FILM_SCHOOL, markbuildsbrands_transcripts, UGO_CORPUS | LOG | These four totals are the durable baseline every later regression comparison divides by. | already-done — Auditor-verified (V) same day (PLAN_CRITIQUE lines 19,70). Note the totals are summary-point counts, HIGHER than the parent counts in the critique's sec.2 table (e.g. v2 130,503 parents vs 86,880 summary-required) — different denominators, not a contradiction; 'summary-required parent' excludes non-required rows. The back-level-on-capture-fields clause is accurate per V probe (pair latent coverage = 0). | Fresh verify run printing the same four totals (or explained deltas from new ingests), exit 0. |
| P0S:L364 | [x] Remove or supersede legacy empty-model summary points after backfill. (2026-07-13 restamp: verify green, 0 empty-model summaries remain on any corpus; prio… | x | done-verified | Accept receipt (2026-07-13 restamp, verify green). Re-verification rides the same command as L359 — a single `p0_1_summary_integrity.py verify` re-run covers both. | script (executed); junior-agent for restamp re-runs | Qdrant summary points all corpora (summary_model payload); Mongo summary rows | LOG | Legacy placeholders left in place would silently compete with valid summaries in vector space. | already-done — Covered by the same V receipt as L358/L359 (0 empty-model on any corpus, auditor-run). The 'prior in-progress reprojection completed' clause resolves the stale IN PROGRESS tags called out in critique C4 — this line is the Rule 3 restamp doing its job. | `p0_1_summary_integrity.py verify` exit 0 with empty_model_points == 0 on all four corpora. |
| P0S:L365 | verify green, 0 empty-model summaries remain on any corpus; prior in-progress reprojection completed. | na | done-verified | Re-check the number: run the verify and assert empty_model_points == 0 per corpus (metric at p0_1_summary_integrity.py:516). | junior-agent (script re-run) | backend/scripts/p0_1_summary_integrity.py:516; Qdrant summary points | LOG | Zero empty-model is the standing invariant ('no empty-model placeholder represented as a valid abstractive summary') made measurable. | already-done — Fact-claim duplicating L364's receipt; independently V'd by the plan critique. Canonical term per registry_leftover: 'empty summary_model placeholder' (aliases: explicit empty-model, unmodeled summary). | Verify output: empty_model_points == 0 x4 corpora, exit 0. |
| P0S:L367 | [ ] Verify every valid summary has child IDs, boundaries, model, schema, validation status, and evidence-backed semantics. | open | build | Extend p0_1_summary_integrity.py verify with a per-summary field audit: source_child_ids non-empty and resolvable to Mongo chunks rows; child_boundaries provenance present; summary_model non-empty; schema_version present; validation status present; semantics fields (key_terms/mechanisms/concept_tags, and post-S4 latent_concepts/temporal_class) evidence-backed. Run it AFTER the S4 consolidated pass so the pair's back-level rows are not certified prematurely. | junior-agent builds the verify extension; script executes it | backend/scripts/p0_1_summary_integrity.py; Mongo parent_chunks (source_child_ids, summary fields); Qdrant summary payloads | S4 | A summary without child provenance cannot be trusted as hydration evidence and cannot be audited for hallucination. | feasible — TERMINOLOGY WARNING (registry_hierarchy 'boundaries', verdict DISTINCT): 'boundaries' here means Ghost A child_boundaries / durable source_child_ids provenance on parent rows — NOT summary_tree section_range (that is P0.2 L391's sense). One validator for both would be wrong. Also 'child IDs' means source_child_ids (chunk ids), not tree child_node_ids. Sequencing: certifying 'evidence-backed semantics' before S4 would violate the spirit of Standing Rule 1 — the pair's semantics fields are known back-level (PLAN_CRITIQUE C1). Base fields (IDs/model/schema) could be spot-checked earlier as a gap-filler, but the closing run belongs after S4. | Extended verify run, exit 0, reporting per-corpus: 100% summaries with resolvable source_child_ids, model, schema_version, validation status; 0 rows failing the semantics-evidence check; dated restamp on the ledger line. |
| P0S:L369 | [ ] Verify document-summary trees roll up only validated children. | open | build | Build a rollup-provenance validator: for every summary_tree rollup node, assert all member parent_ids reference parents whose summaries are validated (non-empty model + validation status), and that no rollup includes quarantined/superseded parents. Run after S4 (pair summaries regenerate there) and after any backfill_tree_concepts re-run. | junior-agent (validator script); script execution | Mongo summary_tree (TreeNode.parent_ids per registry_hierarchy); Mongo parent_chunks; backend/services/ingestion/summary_tree.py | S4 | Rollups built over invalid children launder placeholder content into the tree-descent retrieval path. | feasible — TERMINOLOGY WARNING x2 (registry_hierarchy): (1) 'document-summary trees' here = the per-doc summary_tree (canonical summary_tree), NOT doc_profile/Tier-0 doc_summaries cards — three referents share 'doc summary'. (2) On TreeNode, parent_ids = L1 MEMBER parent-chunk ids and child_node_ids = tree children; 'roll up only validated children' means validating parent_ids members, despite the word 'children'. A validator keyed on child_node_ids would invert the hierarchy. Feasible now mechanically, but a pre-S4 pass would need re-running; slot with S4's verify. | Validator run, exit 0: 0 rollup nodes referencing unvalidated/superseded parents, per corpus; counts logged per node_type. |
| P0S:L373 | [x] explicit_empty_model == 0 among retrieval-eligible summary points. (verified 2026-07-13: 0 across all four corpora) | x | done-verified | Accept receipt — verified 2026-07-13 (0 across all four corpora, auditor V). Standing re-check rides any future verify re-run (same command as L359). | script (p0_1_summary_integrity.py verify) | Qdrant summary points, retrieval-eligible set, all corpora | LOG | Direct acceptance gate for the P0.1 invariant that placeholders never reach retrieval. | already-done — Verified same-day by both the ledger restamp and the plan critique's independent V run. The metric name matches code exactly (empty_model_points / explicit_empty_model, registry_leftover). | Held: verify output explicit_empty_model==0 x4, 2026-07-13. Re-close on demand with a fresh verify exit 0. |
| P0S:L375 | [x] Summary-required coverage is 100% for every strict-ready corpus. (verified 2026-07-13: 86,880/86,880 + 9,453 + 1,009 + 203, all attributed) | x | done-verified | Accept the numeric receipt (86,880/86,880 + 9,453 + 1,009 + 203 all attributed, verified 2026-07-13) but RE-SCOPE the wording at S6: 'strict-ready corpus' has no code referent today — after the S6 readiness split defines strict_ready, re-evaluate this acceptance against the real gate set. | script (verify, done); owner + backend service at S6 for the strict_ready binding | backend/scripts/p0_1_summary_integrity.py; backend/services/.../readiness.py (compute_corpus_readiness); checklist Definition Of Strict Ready (RAPTOR checklist:1252-1268) | S6 | Coverage-vs-readiness binding decides which corpora may legally serve strict retrieval. | risky — Numbers are V-verified; the risk is terminological. registry_readiness: 'strict_ready' is PLAN-ONLY — zero code hits — and ~80% of its definition already lives in compute_corpus_readiness (readiness.py:1105+); building it as a parallel computation is the flagged failure mode. ALSO a word collision: strict_ready is simultaneously the top value of the temporal capability enum (temporal_unavailable\|partial\|strict_ready) — this acceptance means the corpus publication gate, not the temporal tier. As written the acceptance was verified against 'all four corpora' as a stand-in for a gate that doesn't exist yet; S6 must restate it. | Held: coverage numbers 2026-07-13. S6 addendum: strict_ready computed as an extension of corpus_readiness.v2, and this acceptance re-asserted per strict-ready corpus under that definition. |
| P0S:L377 | [ ] Funnel A returns no byte-identical parent replacement presented as a summary. | open | build | Build the probe: sample Funnel A results across the four corpora and assert no returned summary text is byte-identical to its parent chunk text (compare Qdrant summary payload text vs Mongo parent_chunks text for every sampled hit); wire it as an asserting script with non-zero exit on any match. | junior-agent (probe script) | backend/services/retriever/funnel_a.py (read path); Qdrant summary points; Mongo parent_chunks | S0 | A byte-identical 'summary' is an extractive fallback masquerading as abstraction — the exact fraud class P0.1 exists to kill. | feasible — Independent of the S4 capture fields — it tests text identity, not semantics — so it is runnable immediately as a gap-filler (plan §3 parallelizable clause covers P0.2/P0.6 remainders; this P0.1 probe is equally non-blocking). With 0 empty-model rows remaining (V), a residual byte-identical case would come from a model echoing input, which only a live probe catches. | Probe run over N>=500 sampled Funnel A hits per corpus: 0 byte-identical summary/parent pairs; exit 0; ledger restamp with date + sample size. |
| P0S:L379 | [ ] Fast, Hybrid, and Graph recall do not regress after placeholder removal. | open | build | Run the 3-tier recall comparison against the 2026-07-13 baseline (capture_raptor_baseline.py artifacts): Fast (qdrant_only), Hybrid (qdrant_mongo), Graph (qdrant_mongo_graph) on the held-out query set; assert recall deltas >= 0 vs pre-removal baseline. Final closure rides the S14 full regression matrix. | junior-agent (eval run); script (baseline diff) | backend/scripts/capture_raptor_baseline.py artifacts; retriever tiers (RetrievalTier enum); held-out eval set (P1.1) | S14 | Proves placeholder removal purified rather than shrank the evidence pool — the no-regress half of the P0.1 bargain. | feasible — Canonical tier names per registry_leftover: Fast=qdrant_only, Hybrid=qdrant_mongo, Graph=qdrant_mongo_graph ('Graph' alone is OVERLAPPING — qualify as the retrieval tier, not Neo4j subsystem). Baseline exists (task ledger: baseline captured; plan S8 references 'the 2026-07-13 baseline'). Caveat: the CURRENT deployed latency baseline shows Fast degraded at 39.8s (checklist Current Durable Baseline) — recall comparison is still valid, but interleave with S0's warmup fixes to avoid conflating latency noise. Can run early as a gap-filler; S14 is the binding gate. | Eval report: per-tier recall on held-out set, delta vs 2026-07-13 baseline >= 0 (or owner-accepted explanation), attached to the S14 regression matrix record contract. |
| P0S:L383 | [x] Treat parser-emitted `Page N` headings as non-semantic structure. | x | done-unverified | Re-verify the code seam: confirm _is_page_heading/_PAGE_HEADING_RE at backend/services/ingestion/summary_tree.py:288-300 demotes 'Page N' labels (page labels inherit the last real heading) in the deployed image; the behavioral proof is acceptance L400's fixture test — do not double-build. | junior-agent (grep + container check) | backend/services/ingestion/summary_tree.py:288-300 (_is_page_heading, _PAGE_HEADING_RE) | LOG | Page-number pseudo-headings fragment trees into degenerate one-parent sections, the root cause of the 20,942 one-child sections. | already-done — registry_hierarchy ('summary tree' row) cites the shipped symbols at exact lines. No dated receipt on the ledger line, and its behavioral acceptance (L400, new trees don't fragment) is still open — code presence is confirmed, effect is not yet fixture-proven. Note this is one of THREE heading seams (synthetic header injection in b_plus_normalizer.py:131, docling section_heading, this demotion) — the S3 OCR-heading audit must not re-implement it. | Grep receipt in deployed image + L400 fixture test green (that test is the real closer). |
| P0S:L384 | [x] Add deterministic singleton section passthrough IDs. | x | done-unverified | Re-verify the passthrough ID contract: confirm index_summary_tree_nodes sets passthrough_rollup_id/passthrough_parent_ids/passthrough_lexicon_ids (summary_tree.py:171-172,242-243) and _summary_tree_payload projects them to Qdrant (qdrant_writer.py:1205-1212); spot-check 10 one-child sections in Mongo+Qdrant for deterministic, stable IDs across a re-index. | junior-agent (code + data probe) | backend/services/ingestion/summary_tree.py:171-172,242-243; backend/services/storage/qdrant_writer.py:1205-1212; Mongo summary_tree; Qdrant corpus_{cid8}_schemas kind='summary_tree' | LOG | Stable passthrough IDs are what let one-child sections shortcut descent without breaking addressability. | already-done — registry_hierarchy 'passthrough' row confirms all symbols at exact lines. TERMINOLOGY: canonical = 'passthrough section' fields; 'singleton section' is a prose alias with a strong collision — 'Singleton' elsewhere means service-instance singletons (~20 backend hits), and 'passthrough' elsewhere means OpenAI-provider passthrough (chat_orchestrator.py:6642) — grep accordingly. | Probe: 10 sampled one-child sections carry all three passthrough_* fields, identical values in Mongo and Qdrant; IDs unchanged after a no-op re-index of one document. |
| P0S:L385 | [x] Skip duplicate rollup search when a new section has one passthrough child. | x | done-unverified | Re-verify the skip: confirm passthrough_hits consumption in backend/services/retriever/summary_tree_navigator.py:232-290 short-circuits the rollup vector search for matching one-child sections; the quantitative proof is acceptance L397's probe — route re-verification there. | junior-agent (code check) | backend/services/retriever/summary_tree_navigator.py:232-290 | LOG | Eliminates a wasted vector round-trip on 20,942 one-child sections (~97% of all sections) per descent. | already-done — registry_hierarchy cites the consumer at exact lines and explicitly maps it to acceptance L397 ('one-child descent performs no redundant vector search'). Code is in; the acceptance probe is the outstanding proof. Wording note: applies to NEW sections at query time regardless of build date, provided payloads carry passthrough fields (hence L386's backfill). | Covered by L397's receipt (search-count instrumentation showing 0 rollup searches on passthrough hits). |
| P0S:L386 | [x] Backfill passthrough payloads for existing one-child section points. (merged; 28,402 nodes filled + 20,880 passthrough sections backfilled with backups, li… | x | done-unverified | Re-verify the backfill receipt via counts: Mongo summary_tree docs with passthrough_rollup_id set ~= 20,880 (of 20,942 one-child sections; explain the ~62 gap); confirm the backup artifacts from backfill_passthrough_sections.py exist; confirm Qdrant schemas points mirror the fields. | junior-agent (count probe); script backend/scripts/backfill_passthrough_sections.py (executed) | backend/scripts/backfill_passthrough_sections.py; Mongo summary_tree; Qdrant corpus_{cid8}_schemas | LOG | Without the backfill, pre-fix trees never benefit from the passthrough shortcut (acceptance L396). | already-done — Receipt is ledger-stamped (merged, live 2026-07-13) but not independently V'd by the plan critique. IMPORTANT receipt-splitting note: the line folds TWO scripts' numbers — 20,880 passthrough sections = backfill_passthrough_sections.py, while 28,402 nodes filled = backfill_tree_concepts.py (summary_tree.concepts population, a P2.1-adjacent deliverable per registry_latent 'tree concepts'). They are separate backfills sharing a restamp; verify each against its own script. | Two count probes: (1) summary_tree passthrough_rollup_id present on ~20,880 one-child sections + backups on disk; (2) summary_tree.concepts non-empty on ~28,402 nodes; both matching their script logs. |
| P0S:L387 | merged; 28,402 nodes filled + 20,880 passthrough sections backfilled with backups, live 2026-07-13 | na | done-unverified | Re-check both numbers independently: `db.summary_tree.countDocuments({passthrough_rollup_id:{$exists:true}})` ~= 20,880 and `db.summary_tree.countDocuments({concepts:{$exists:true,$ne:[]}})` ~= 28,402; list the backup files each script wrote. | junior-agent (read-only Mongo probe) | Mongo summary_tree; backup artifacts of backfill_passthrough_sections.py and backfill_tree_concepts.py | LOG | These counts are the P0.2 durable-repair receipt every descent-efficiency claim rests on. | already-done — Fact-claim mirror of L386. The two numbers belong to two different backfills (see L386 note; registry_latent 'tree concepts' row attributes 28,402 to the concepts backfill, checklist:159-163). Sanity: 20,880 <= 20,942 known one-child sections (baseline) — consistent. Not independently V'd; a 2-line Mongo probe closes it. | Mongo count outputs matching 20,880 and 28,402 (or explained deltas), plus backup filenames with row counts. |
| P0S:L388 | [ ] Decide whether future singleton sections should be physically omitted or retained as aliases for stable IDs. | open | owner-decision | Put the omit-vs-alias decision to the owner WITH the S3 disposition-matrix sign-off (same sitting): present storage cost (L390's measurement), ID-stability implications, and the fact that today every one-child section is physically retained + stamped. Decision must land before any reingest lane (S3 outcome / S11 burst) builds new trees at scale. | owner (genuine decision); junior-agent prepares the evidence brief | backend/services/ingestion/summary_tree.py (no omit/alias branch exists today); docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md P0.2 | S3 | Determines whether every future tree pays a per-section storage/point tax or a stable-ID aliasing complexity tax. | needs-owner-decision — registry_hierarchy 'singleton' row: the decision is PLAN-ONLY — no omit/alias code branch exists; current behavior = retain + passthrough-stamp. Canonical term: 'passthrough section (one-child section)'; avoid 'singleton' in the implementation (collides with service-instance singletons). Pairing with S3 is natural: S3 already convenes the owner for the per-corpus reingest disposition matrix, and reingest is exactly when new-tree policy takes effect. L390's measurements are the decision's evidence input — sequence L390 first. | Signed decision recorded in the checklist (omit \| alias \| retain-as-is) with rationale + the L390 numbers it was based on; if a code change is chosen, its own build/verify boxes are opened. |
| P0S:L390 | [ ] Measure section/rollup storage and query round trips after migration. | open | build | Measure and record: (a) storage — summary_tree doc count/bytes and Qdrant schemas point count/bytes attributable to one-child sections, per corpus; (b) query round trips — instrument SummaryTreeNavigator descent to count vector searches per query before/after passthrough (or replay a fixed query set and diff search counts). | junior-agent (measurement script + instrumented replay) | Mongo summary_tree; Qdrant corpus_{cid8}_schemas; backend/services/retriever/summary_tree_navigator.py (diagnostics) | S0 | Feeds the L388 owner decision and quantifies the round-trip win the passthrough repair claims. | feasible — Explicitly a plan §3 gap-filler ('P0.2 remainders can fill any gap — they touch nothing above'). All inputs live: 21,432 sections / 20,942 one-child / 23,394 rollups baseline for cross-checking, navigator lane diagnostics already label descent modes (registry_hierarchy 'hierarchy descent'). Read-only; no freeze conflict. | Measurement artifact: per-corpus table (one-child section storage share; mean vector searches/query pre-vs-post passthrough on a fixed replay set), committed to CONTINUITY or the checklist as the L388 evidence brief. |
| P0S:L391 | [ ] Validate document profile, section, rollup, parent, and child boundaries on PDF, transcript, Markdown, HTML, and structured-list fixtures. | open | build | Build the fixture suite: one small document per format (PDF, transcript, Markdown, HTML, structured-list) ingested through the real pipeline in a test corpus; assert document profile node, section grouping, rollup membership (parent_ids), parent extents, and child extents match golden expectations. House under backend/tests with non-zero exit on fail. | junior-agent (fixtures + asserting tests) | backend/tests (new fixtures); backend/services/ingestion/{docling_adapter.py,tier_chunker.py,summary_tree.py}; disposable test corpus | S0 | Boundary correctness across formats is the only guard that new-format ingests build sane trees before paid passes run over them. | feasible — Gap-filler per plan §3, and quietly load-bearing for S3/S11 (a reingest lane without fixture coverage re-buys risk). TERMINOLOGY WARNING (registry_hierarchy 'boundaries', DISTINCT): the P0.2 sense = tree-node extents (TreeNode.section_range) + parent/child chunk extents — NOT Ghost A child_boundaries (that is L367's P0.1 sense). Also 'parent/child' here: rollup membership is parent_ids (member parent-chunk ids) vs tree structure child_node_ids — assert both without conflating. YT-transcript and table/code lanes already have exemplary chunker handling (chunker-spectrum verdict), so transcript fixtures should pass today. | Fixture test file(s) in repo, run GREEN in the container (exit 0), covering all 5 formats with golden assertions on profile/section/rollup/parent/child extents; L400's page-heading case included. |
| P0S:L396 | [ ] Existing trees use the passthrough path without re-embedding. | open | build | Probe that the passthrough backfill was payload-only: for a sample of backfilled one-child sections, fetch the Qdrant point vectors before/after references (backups hold pre-state) and assert vectors are unchanged; confirm query-time descent takes the passthrough branch for those sections (navigator diagnostics). | junior-agent (vector-identity probe) | Qdrant corpus_{cid8}_schemas points; backfill backups; backend/services/retriever/summary_tree_navigator.py diagnostics | S0 | Re-embedding 20k sections would have been an unbudgeted paid pass; proving payload-only repair certifies the migration's cheapness claim. | feasible — Gap-filler. backfill_passthrough_sections.py wrote backups (ledger receipt), giving pre-state for the identity check; set_payload-style updates leave vectors untouched by construction, so this is a cheap confirmation rather than a risky investigation. | Probe output: N sampled backfilled sections, vector checksum identical pre/post, passthrough branch taken on live descent for each; exit 0. |
| P0S:L397 | [ ] One-child descent performs no redundant vector search. | open | build | Instrumented replay: run a fixed query set that routes through one-child sections and assert (via navigator search-count diagnostics or Qdrant request logs) that zero rollup vector searches are issued where passthrough_hits applies. | junior-agent (instrumented replay probe) | backend/services/retriever/summary_tree_navigator.py:232-290; Qdrant search logs / lane diagnostics ('descent' labels) | S0 | This is the acceptance that converts L385's code into a measured latency/round-trip win. | feasible — Gap-filler; the skip logic and its diagnostics exist (registry_hierarchy 'passthrough' + 'hierarchy descent' rows: lane diagnostics label descent modes). Shares its replay harness with L390(b) — build once, close both. | Replay report: per-query search counts with 0 redundant rollup searches on passthrough sections; exit-0 assertion; ledger restamp. |
| P0S:L398 | [ ] Broad retrieval still reaches every parent formerly reachable through the duplicate section. | open | build | Reachability parity probe: for each backfilled one-child section, assert the union of parents reachable via the passthrough path equals the parent set formerly reachable through the duplicate rollup (compare passthrough_parent_ids against the rollup node's parent_ids); then a broad-retrieval smoke over sampled queries confirming no parent disappears from results. | junior-agent (set-parity probe + smoke) | Mongo summary_tree (passthrough_parent_ids vs rollup parent_ids); retriever broad path (Funnel A / tree descent) | S0 | The shortcut must be an optimization, not a recall hole — every formerly reachable parent must stay reachable. | feasible — Gap-filler; purely deterministic set comparison plus a smoke run — both id sets are durable fields (registry_hierarchy 'passthrough'). Reminder from the 'parent' collision note: both sides are parent-CHUNK id lists, not tree ancestry; compare like with like. | Parity report: 100% of one-child sections with passthrough_parent_ids == rollup parent_ids (set equality), 0 recall-lost parents in the smoke set; exit 0. |
| P0S:L400 | [ ] New trees do not fragment at page-number headings. | open | build | Include a page-number-heading fixture in the L391 suite: a document whose parser output contains 'Page N' headings mid-flow; assert the built tree contains no section keyed on a Page-N heading and that affected parents inherit the last real heading (per _is_page_heading behavior). | junior-agent (fixture test, rides L391) | backend/tests (fixture); backend/services/ingestion/summary_tree.py:288-300 | S0 | Behavioral proof of L383 — prevents regeneration of the degenerate one-child-section pathology on every future ingest. | feasible — Directly testable against shipped code (_is_page_heading, _PAGE_HEADING_RE); zero data-state dependencies. Cheapest of the four P0.2 acceptances — fold into L391's suite rather than a separate harness. Relevant to S3's reingest decision: if ecom reingests, this test is the guard that new trees come out clean. | Fixture test GREEN: tree built from the Page-N fixture has 0 sections with page-label headings and expected section count; runs in the container, exit 0. |
