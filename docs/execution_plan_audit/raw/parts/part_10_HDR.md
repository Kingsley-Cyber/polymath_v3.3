### Header/Standing Rules/Legend/Invariants/Baseline (frozen lines 1-96, chunk HDR)

| ID | Claim (verbatim) | Status | Disposition | Auditable action | Who | Where | When | Why | Feasibility | Required receipt |
|---|---|---|---|---|---|---|---|---|---|---|
| HDR:L3 | Last updated: 2026-07-13 | na | done-verified | Re-verify 'Last updated: 2026-07-13' matches the newest ledger-touching commit: `git -C /Users/king/polymath_v3.3 log -1 --format=%ad -- docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md`; restamp on every ledger edit per Standing Rule 3. | script (ledger-edit hygiene step) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:3 | LOG | A stale date silently voids the header's receipts-refresh contract for every downstream reader. | already-done — Verified: file header reads 2026-07-13 and the last two checklist edits (327da45 S0, 336dc65 S3) are dated 2026-07-13. Accurate as of this critique. | git log -1 date of the checklist file == header date; restamped on next edit. |
| HDR:L4 | Baseline commit: `d3159b8` | na | done-verified | Accept receipt d3159b8 — verified 2026-07-13: `git cat-file -t d3159b8` returns commit in /Users/king/polymath_v3.3 AND docs/baselines/BASELINE_2026-07-13.json.git_commit == d3159b816460f1e0b0830f6682cbcb30aa0675de. | script | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:4; docs/baselines/BASELINE_2026-07-13.json | LOG | The baseline commit anchors every before/after comparison; a wrong ref poisons all deltas. | already-done — Both probes run and pass. TERMINOLOGY (registry_leftover 'baseline commit'): 'baseline' is 3-way overloaded — this git ref vs the census BASELINE_*.json vs the eval EVAL_*.json baselines. Always qualify which baseline a sentence means. | git cat-file -t d3159b8 -> commit; BASELINE_2026-07-13.json git_commit field matches. |
| HDR:L8 | The dependency-ordered execution plan derived from this ledger is docs/PLAN_CRITIQUE_2026-07-13.md (S0–S14, with verified data-state receipts and the critique of prior … | na | done-verified | Accept receipt — verified 2026-07-13: docs/PLAN_CRITIQUE_2026-07-13.md exists with the full S0–S14 table (lines 77–93), V/L evidence labels, and critique C1–C4. | script | docs/PLAN_CRITIQUE_2026-07-13.md; docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:6-12 | LOG | The plan file is the sole ordering authority; its existence and content are load-bearing for every dispatch. | already-done — Read this session; S0–S14 present with verified data-state receipts (section 2). One receipt inside it is already stale: line 71 'shelf_reserve as a symbol does not exist' is superseded by merge f049041 (registry_librarian shelf_reserve row) — the plan file itself needs Rule-3 restamping when next edited. | File present; S0–S14 rows enumerable; known-stale line 71 annotated on next plan-file edit. |
| HDR:L10 | Execute in S-order; a step's checklist anchors are listed per row. This ledger stays the item-level source of truth; the plan file is its ordering. | na | standing-invariant | Enforce at every dispatch: before starting any step, re-read the step's checklist anchors from the ledger on disk and confirm the step's S-slot is the lowest unfinished S; record ledger-line anchors in the work item. | junior-agent (dispatch preamble), enforced by orchestration prompt | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md + docs/PLAN_CRITIQUE_2026-07-13.md | CONT | Ledger-as-item-truth plus plan-as-ordering prevents the C1 class of out-of-order paid work. | feasible — Mechanically checkable: S0 done (327da45), S3 evidence gathered awaiting owner sign-off (336dc65) — current lowest open slots are S1/S2 (parallelizable with S3 per plan line 95), which matches open tasks #18/#20. Consistent with the repo CLAUDE.md no-cached-plan rule. | Each dispatched work item cites its S-slot + ledger line anchors read from disk at dispatch time. |
| HDR:L16 | Capture-before-rebuild: no summary/lexicon/card rebuild may run while an ADOPTED capture hook targeting the same rows is unbuilt. A rebuild must list the open capture … | na | standing-invariant | Before ANY summary/lexicon/card rebuild, run a pre-flight that lists ADOPTED-but-unbuilt capture hooks for the target rows (currently: T-HOOK-2 temporal_class/time_expressions — S1 unbuilt) and blocks unless an explicit owner waiver line is added to the ledger. | junior-agent pre-flight + owner (waiver only) | Ghost A summary contract seam (backend/services/ingestion/summary_semantics.py), corpus_lexicon rematerialization, services/librarian/card_builder.py; ledger Standing Rules block | CONT | Prevents re-buying paid passes (C1 cost: ~2,000 back-level paid v2 regenerations + a dedicated S4 pass). | feasible — Concrete right now: S1 (T-HOOK-2) is unbuilt, so this rule currently BLOCKS any pair summary regen and the S8 card rebuild until S1/S4 land — exactly the plan's ordering. TERMINOLOGY: 'rebuild' spans three distinct seams (summary regen, lexicon rematerialization, card projection); the pre-flight must check hooks per-seam, not globally. | Every rebuild run log opens with 'open capture hooks for this seam: [...]' and either 'none' or an owner-waiver ledger line; violation count stays 1 (C1/C2). |
| HDR:L19 | (Violated once — critique C1/C2; never again.) | na | done-verified | Accept receipt — verified: PLAN_CRITIQUE_2026-07-13.md C1 (lines 99-101, rebuild-before-capture on P0.1 regen) and C2 (lines 102-103, 673 cards built with empty latent input) document the single violation. | script | docs/PLAN_CRITIQUE_2026-07-13.md:99-103 | LOG | The recorded violation is the evidentiary basis for the standing rule's adoption. | already-done — Both critique entries read and match the claim: pair latent coverage 0, only 639/130,503 v2 parents carry latent_concepts, zero temporal_class anywhere (critique section 2, V). | C1/C2 text present in the plan file; 'never again' enforced via HDR:L16's pre-flight receipt. |
| HDR:L20 | No dark fields: every captured field ships with its first consumer, or a named consumer + checklist anchor in the same phase; otherwise the ledger must mark it dark data. | na | standing-invariant | Maintain a dark-data ledger check: for every captured field, grep for a reader; currently mark and track: (1) latent_concepts[].aliases — parsed/clamped, read by NOTHING (consumer lands S5); (2) latent_concepts dropped from Qdrant summary payloads (qdrant_writer.py:877-928 whitelist); (3) page_start — writers+readers exist, 0 rows populated on all 4 corpora (only a reingest lane can fill it, S3 matrix). | junior-agent (audit script per phase close) | backend/services/ingestion/summary_semantics.py, backend/services/storage/qdrant_writer.py:877-928, docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md | CONT | Dark fields silently invalidate every measurement taken while they accumulate (critique C3). | feasible — The three current dark items are registry-confirmed (registry_latent collision note; registry_hierarchy page_start row 'implemented-but-dead'). S5 is the named consumer for aliases — per this rule S5 must CONSUME the existing captured aliases, not re-capture them. | Ledger contains an explicit 'dark data' marker for each consumer-less field, cleared only by a merged consumer with a checklist anchor (aliases cleared at S5). |
| HDR:L23 | Receipts refresh tags: any status tag (IN PROGRESS/PARTIAL) contradicted by a later verification is stale by definition; ledger edits re-run the relevant verify and re… | na | standing-invariant | Apply the rule NOW to three known-stale spots: (1) P0.1's two IN PROGRESS tags (verify PASSES all 4 corpora — critique line 19/70); (2) the Current Durable Baseline empty-summary_model lines L77-L84 (contradicted by the same green verify: 0 empty-model, 86,880+9,453+1,009+203 attributed); (3) L93's embedder /health sentence (contradicted by S0 receipt 327da45). Re-run the relevant verify, restamp dates. | junior-agent (ledger edit + verify re-run) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md (P0.1 tags, lines 75-95); backend/scripts/p0_1_summary_integrity.py | CONT | Stale tags break the header's contract that a checkbox equals deployed + reconciled + verified. | feasible — All three stale spots have fresh verifications on record (critique V receipts + S0 commit message with live :8082/health output). Cheap ledger edit + one script re-run. | p0_1_summary_integrity.py verify exit 0 pasted with date; ledger diff removing/restamping the three stale passages. |
| HDR:L26 | One migration per seam: migrations touching the same artifact are co-scheduled (P2.5 + T-MAIN edges; S5 rollup + S7 facet cleanup payloads). | na | standing-invariant | Enforce co-scheduling at plan level: P2.5 typed-signature edge migration ships in the same migration window as T-MAIN versioned RELATES_TO edges (S11/S12 pairing); S7 facet-cleanup payload rewrites ride S5's lexicon/payload rematerialization (payloads rewrite once). Pre-flight any migration by asking 'what else is scheduled to touch this artifact?' | junior-agent (migration planner) + owner sign-off on migration windows | Neo4j edges (RELATES_TO), Qdrant summary/parent payloads, corpus_lexicon collections | CONT | Double-touching durable artifacts doubles cost and doubles the chance of a half-migrated store. | feasible — The two named pairings match the plan rows exactly (S5+S7 payload batch, S12 'co-scheduled with P2.5 edge migration (one migration)'). TERMINOLOGY: 'seam' here means durable artifact, not code seam — the rule binds data migrations, not code merges. | Each migration PR/run log names its seam and lists co-scheduled migrations; S5 and S7 payload rewrites appear as ONE Qdrant pass in run logs. |
| HDR:L28 | Read-before-act: agents re-read the relevant section of THIS FILE from disk at every phase start and before dispatching work — never from session memory (see repo CLA… | na | standing-invariant | Enforce mechanically: every phase-start and dispatch message must quote the ledger section (with line numbers) as read from disk in that turn; forbid acting on remembered plan state. | junior-agent (all agents), per repo CLAUDE.md north-star section | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md; repo CLAUDE.md (@6f9becb codification) | CONT | Session-memory execution is how adopted hooks got skipped while their seam was hot (C1). | feasible — Already codified in polymath CLAUDE.md (no-cached-plan-execution, commit 6f9becb per user memory). Verification is procedural: transcripts show a fresh Read of the ledger before each dispatch. | Dispatch transcript contains a same-turn disk read of the cited ledger lines. |
| HDR:L31 | Scope: ingestion, hierarchy, retrieval, librarian behavior, concept vocabulary, GLiNER/RunPod parity, lifecycle hygiene, and all three retrieval tiers. | na | informational | Re-check scope completeness against the ledger's section list whenever a new work family is added; no build step. | owner (scope is a decision) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:31-32 | LOG | Declared scope bounds what the regression matrix and strict-ready definition must cover. | already-done — The eight scope areas map 1:1 onto existing checklist sections (ingestion P0.x, hierarchy P0.2, retrieval P1.x, librarian phases, concept vocabulary P1.7/P2.1, GLiNER/RunPod parity P2.6/P2.7, lifecycle P0.6, three tiers = invariant 1 + regression matrix). | None beyond the ledger itself; scope change requires an owner-signed ledger edit. |
| HDR:L34 | This file is the implementation ledger. A checkbox is complete only when the code is deployed, durable data is reconciled where required, and the listed acceptance te… | na | standing-invariant | Enforce the completion definition at every box flip: a [x] edit must cite (a) deploy receipt (image rebuild/restart or sidecar redeploy), (b) durable-data reconciliation where applicable, (c) acceptance test run with exit code. | junior-agent (box-flip hygiene) + owner spot-check | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:34-36 and every checkbox edit | CONT | 'Code existing is not completion' is the direct antidote to the dark-code failure (C3: 6 boxes sat IN CODE unmerged). | feasible — S0's commit 327da45 is the model receipt (live :8082/health output + in-container suite counts + probe exit 0 in the commit message). Matches user's assert-before-commit and deploy-train rules (plan line 75). | Every [x] flip commit message contains deploy + reconcile + acceptance evidence, S0-style. |
| HDR:L40 | `[x]` implemented, tested, deployed, and verified. | na | informational | No build step; apply the definition when auditing: treat any [x] lacking a deploy/verify receipt as suspect and re-verify per Standing Rule 3. | junior-agent (auditors) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:40 | CONT | Shared tag semantics keep multi-agent audits comparable. | feasible — Definition is consistent with L34's completion contract; no conflict found in registries. | None — definitional. |
| HDR:L41 | `[ ]` not complete. | na | informational | No build step; auditors treat [ ] as open regardless of how much code exists (IN CODE annotations do not flip boxes). | junior-agent (auditors) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:41 | CONT | Prevents dark code from masquerading as done. | feasible — Consistent with C3's finding; the S0 pass demonstrated the correct flip path (IN CODE -> live-verified -> [x]). | None — definitional. |
| HDR:L42 | Partial means some sub-items are complete but the parent outcome is not. | na | informational | No build step; when reporting, count Partial parents as OPEN in rollups (as the plan critique's status table does). | junior-agent (auditors) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:42 | CONT | Parent-outcome honesty stops sub-item progress from inflating completion. | feasible — Live examples exist and are correctly tagged: P0.4 contradiction checks PARTIAL @f2fb6e2 (re-scoped in S0 commit, explicitly NOT built), P0.6 lease matrix PARTIAL. | None — definitional; rollup tables count Partial as open. |
| HDR:L43 | Experiment means no production adoption until before/after evaluation. | na | informational | No build step; gate any Experiment-tagged item (P3.4 quantization, P3.5 reranker serving) behind a recorded before/after evaluation prior to production adoption; clone-first where tagged. | junior-agent (executor) + owner (adoption decision) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:43; P3.4/P3.5 sections | CONT | Keeps experiments from leaking into production without an eval receipt. | feasible — TERMINOLOGY (registry_eval 'before/after'): the before/after GATE is itself to-be-built — nearest seam is re-running run_heldout_eval per tier and diffing EVAL_*.json summaries; do not confuse with 'candidate counts before/after fusion' (per-query stage counts, checklist:1246). | Any experiment adoption cites paired EVAL_*.json (before/after) artifacts. |
| HDR:L44 | Migration means existing durable artifacts must be changed or rebuilt. | na | informational | No build step; when a Migration-tagged item runs, apply Standing Rule 4 (one migration per seam) and take pre-migration backups as done for P0.x (docs/baselines/p0_*_backups pattern). | junior-agent (migration executor) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:44; docs/baselines/p0_*_backups/ | CONT | Migration semantics distinguish durable-data work (needs reconciliation + backup) from code work. | feasible — Backup convention already exercised 4 times (p0_1/p0_2/p0_5/p0_8 backup dirs exist in docs/baselines/). Consistent with the user's non-destructive snapshot+restore rule. | Every migration run has a pre-image backup path + post-migration reconcile count in its log. |
| HDR:L48 | Every retrieval change is tested through Fast (`qdrant_only`), Hybrid (`qdrant_mongo`), and Graph (`qdrant_mongo_graph`). | open | standing-invariant | Enforce per retrieval-change PR: run the three-tier probe (three_tier_eval / probe_tier_latency) across RetrievalTier.qdrant_only, qdrant_mongo, qdrant_mongo_graph and attach per-tier results; assert requested_tier == effective_tier in diagnostics to catch silent graph->hybrid collapse. | junior-agent (per-change gate); backend eval scripts | backend three_tier_eval.py, eval_metrics.py:214 (tier alias map), models/_schemas_legacy.py:1204-1205 (requested_tier/effective_tier); chat_orchestrator.py:3952-3953 | CONT | A change validated on one tier routinely regresses another; tier collapse hides Graph regressions entirely. | feasible — Harness exists (EVAL_2026-07-13_*.json captured per tier). TERMINOLOGY (registry_retrieval 'tier'): 'tier' is 4-way overloaded — RetrievalTier (this invariant), SourceChunk.source_tier, Tier-0 routing (NOT a RetrievalTier), and tier_chunker Tiers A/B/C. 'Fast' is the checklist alias for qdrant_only (eval_metrics.py:214 is authoritative). | Per-change: three EVAL/probe artifacts (one per tier) with requested_tier==effective_tier asserted; graph tier not silently collapsed. |
| HDR:L50 | Cross-corpus tests preserve the exact user-selected corpus boundary. | open | standing-invariant | Enforce via the cross-corpus heldout rows + an assertion that every returned evidence row's corpus_id is a member of the user-selected corpus_ids set (no leakage in, no silent drop of a selected corpus). | junior-agent (eval harness assertion) | retriever corpus_ids filtering; heldout suite cross-corpus/cross-corpus-irrelevant shapes; reservation_policy corpus_floor diagnostics | CONT | Corpus boundary is the user's explicit trust boundary; leakage is a correctness and privacy failure. | feasible — P0.3 is 10/11 done with only the acceptance row open ('cross-corpus retains evidence from each necessary corpus' — critique table row P0.3), so the machinery exists; the standing gate is the eval assertion. Note 'shelf' in P0.3's corpus_floor.skipped diagnostics means CORPUS (registry_librarian 'shelf' collision). | Cross-corpus eval rows pass with per-row corpus_id membership assertion; corpus_floor.skipped reports reasons for any omitted selected corpus. |
| HDR:L51 | Retrieval remains top-down: corpus/shelf -> document -> hierarchy -> parent/child evidence. | open | standing-invariant | Enforce order-of-operations in code review + trace audit: Tier-0 document routing precedes tier retrieval; descent uses document_gated_adaptive_tree_descent; no lane may fetch child evidence for documents never seated/routed. | junior-agent (reviewer + trace probe) | Tier-0 routing (polymath_doc_summaries projection), summary_tree descent (document_gated_adaptive_tree_descent), retriever lanes | CONT | Top-down is the owner architecture's core shape; bottom-up leakage reintroduces the flat-chunk soup RAPTOR work exists to kill. | feasible — TERMINOLOGY, two collisions: (a) 'corpus/shelf' — 'shelf' means CORPUS here (P1.4 sense), not the P1.5 shelf-role bucket (registry_librarian); (b) 'hierarchy' descent from shelf_reserve seats is NOT yet wired — descent-from-Tier-0 is built, descent-from-seats awaits S8 (registry_hierarchy 'hierarchy descent' row). The invariant is enforceable today on the built path. | Retrieval trace for a probe query shows routing -> document -> tree descent -> parent/child order with no un-routed child fetches. |
| HDR:L53 | The original query remains a protected retrieval lane. | open | standing-invariant | Enforce: the role=='original' recall lane is always present in the query plan and its candidates are never displaced by expansion lanes; assert in planned-pool fusion tests that removing all expansion lanes still yields the original lane's results. | junior-agent (fusion test suite) | retriever/query_plan.py (QueryPlanV2 lanes), fuse_planned_pools, ranking_policy | CONT | The original query is the only lane whose intent is certain; losing it converts expansion errors into total failures. | feasible — TERMINOLOGY (registry_retrieval 'protected lane'): canonical = protected_recall_lane (role=='original'). Three unrelated 'protection' mechanisms share the verb — recall-lane protection (this), reservation seats (reservation_policy.py, P0.3), and per-doc-cap protect_fn. Tests must target the lane sense specifically. | Asserting test: original lane present in every plan; fusion output non-empty and stable when expansion lanes are ablated. |
| HDR:L54 | Query expansion cannot replace or semantically drift from the original query. | open | standing-invariant | Enforce via the existing grounded-expansion gate: expansions pass the grounded threshold, rejected_expansions and lexicon-ID rejections are logged, and the original lane (L53) is never rewritten; S5's alias-registry consumption must feed THROUGH this gate, not around it. | junior-agent (S5 implementer + gate tests) | grounded vocabulary expansion gate (grounded threshold + rejected_expansions, checklist:597); vocabulary.py; future versioned alias registry (S5) | CONT | Ungated expansion is the classic drift vector that turns one wrong alias into corpus-wide wrong answers. | feasible — Gate machinery ALREADY exists (registry_alias 'query expansion' row: canonical = grounded vocabulary expansion). The live risk is S5 wiring registry aliases as a bypass; registry explicitly flags this failure mode (b). | Expansion trace shows grounded-threshold pass/reject counts per query; forbidden-vocabulary control rows (three_tier_eval) stay green. |
| HDR:L56 | Final evidence retains `corpus_id`, `doc_id`, parent/child identity, heading/boundary metadata, and source provenance. | open | standing-invariant | Enforce with a payload-contract test: every final evidence row carries corpus_id, doc_id, parent/child identity, heading/boundary metadata, and source provenance; add this as a record-contract column in the 3-tier regression matrix (its 8-field record contract). | junior-agent (contract test) | final evidence assembly (chat_orchestrator evidence packets), Qdrant payloads, regression matrix record contract (checklist:1219-1250) | CONT | Provenance-less evidence can't be cited, deduped, corpus-bounded, or audited. | feasible — TERMINOLOGY (registry_hierarchy 'boundaries' — worst collision in family): 'boundary' spans child_boundaries (Ghost A provenance), chunker split boundaries, tree section_range, and P0.8 storage boundary. The contract test must name WHICH boundary fields are required per row kind, or it will pass vacuously. | Asserting test iterating final evidence rows across all three tiers: zero rows missing any named field; wired into the S14 matrix. |
| HDR:L58 | Summary, vocabulary, graph, and generated user-language artifacts carry explicit model/engine, schema version, evidence, and validation status. | open | build | Enforce at the writer boundary: extend P0.8 schema validators so summary/vocabulary/graph/generated artifacts fail closed without model/engine, schema version, evidence refs, and validation status; the typed-model writer boundary rides the S1 contract touch (plan S1 row). | backend service (schema_validators) + junior-agent (S1) | backend/services/storage/schema_validators.py; Ghost A summary contract; corpus_lexicon entries; Neo4j write path | S1 | Attribution fields are what made the P0.1 empty-model sweep detectable at all; unattributed artifacts are unauditable. | feasible — P0.8 is 3/4 done; the open box is exactly 'typed-model acceptance at Mongo writer boundary' (critique table), and the plan co-schedules it with S1 to touch the contract file once. Summary side already carries summary_model (P0.1 verified 100% attributed). | Writer-boundary test: an artifact missing model/engine or schema_version is REJECTED (non-zero/raised), run green in-container; S1 commit cites it. |
| HDR:L60 | No empty-model or extractive fallback placeholder is represented as a valid abstractive summary. | open | standing-invariant | Keep the P0.1 verify as a standing gate: schedule/require `p0_1_summary_integrity.py verify` (all 4 corpora, exit 0, 0 empty-model) before and after any summary-writing pass; empty-model placeholders may never re-enter via backfill or fallback paths. | script (backend/scripts/p0_1_summary_integrity.py) run by junior-agent per summary pass | Mongo parent_chunks / Qdrant summary points (summary_model field), all 4 corpora | CONT | Placeholder summaries poison Funnel-A breadth and every rollup above them. | feasible — Gate is proven: verify PASSED all 4 corpora on 2026-07-13 (critique line 70: 0 empty-model; 86,880+9,453+1,009+203 attributed). The invariant is now regression-prevention. Canonical term: 'empty summary_model placeholder' (registry_leftover). | p0_1_summary_integrity.py verify exit 0 with 0 empty-model counts, attached to every summary-writing run (S4 especially). |
| HDR:L62 | No query- or document-specific topic rules are hardcoded into retrieval. Curated aliases belong in versioned data, not Python conditionals. | open | build | Two-part enforcement: (1) standing review rule — no query/document-specific conditionals in retrieval code paths; (2) S5 migrates the THREE existing curated alias stores (CONCEPT_ALIASES query_semantics.py:527, entity_aliases.json, canonical_families.json) into the versioned alias registry rather than adding a fourth store. | junior-agent (S5 implementer + reviewers) | backend/services/retriever/query_semantics.py:527 (CONCEPT_ALIASES); neo4j_writer.py:43,46 (entity_aliases.json, canonical_families.json); future versioned alias registry | S5 | Hardcoded topic rules are unversionable, untestable per-corpus, and rot silently. | feasible — TERMINOLOGY COLLISION (registry_alias 'curated aliases'): the phrase names BOTH the retrieval-side CONCEPT_ALIASES (what this invariant means) AND the graph-side canonical_families mechanism (different keyspace: canonicalize_entity_name/NFKD vs normalize_identity). S5 must keep the two keyspaces distinct while versioning both; merging them would corrupt the vector-graph join (cf. entity_id divergence, 'Box2D-Lite' example). | grep of retriever services shows zero topic-term conditionals; CONCEPT_ALIASES literal removed from Python and served from the versioned registry with schema version + provenance; asserting test green. |
| HDR:L64 | Negative controls must fail closed when the corpus lacks the answer. | open | standing-invariant | Enforce via heldout negative_control shape rows: unanswerable-in-corpus questions must produce refusal (REFUSAL_RE match), tested on ALL THREE tiers; keep this distinct from the forbidden-vocabulary control in three_tier_eval. | junior-agent (eval harness) | run_heldout_eval negative_control rows; chat answerability gate (services/answerability_tuning.py); regression matrix row 9 | CONT | Failing open on absent knowledge is the worst RAG failure mode — confident fabrication. | feasible — TERMINOLOGY (registry_eval 'negative controls'): two different assertions share the phrase — (i) refusal of unanswerable questions (THIS invariant) and (ii) forbidden-vocabulary expansion control. Also note the answerability gate is intentionally TWO mirror implementations (chat refusal vs retriever sufficiency, registry_retrieval); tune shared knobs in answerability_tuning.py, never one side alone. | Negative-control heldout rows pass (refusal) on qdrant_only, qdrant_mongo, qdrant_mongo_graph in the per-change three-tier run. |
| HDR:L65 | Pressure gates, provider cooldowns, durable leases, and idempotent replay remain active during all backfills. | open | standing-invariant | Pre-flight every backfill run with a config assertion that pressure gates, provider cooldowns, durable leases, and idempotent replay are ACTIVE (not env-defaulted off); close the two open lease-matrix cells (restart-before-expiry, purge-beyond-lease) before the next mass pass (S4). | backend service (ingestion worker) + junior-agent (P0.6 lease tests) | ingestion worker lease/gate machinery; backend P0.6 test matrix; S4/S11 batch runners | CONT | The S4 paid pass and S11 burst are exactly where an inactive gate converts a bug into a bill. | feasible — Machinery exists (P0.6 is 11 done / 1 partial); the PARTIAL is the lease test matrix — two untested cells named in the critique table. Aligns with user's silent-fallback-accounting rule (preflight canary before batch spend). | Backfill run log opens with gate/cooldown/lease/replay ACTIVE assertions; P0.6 lease matrix cells restart-before-expiry + purge-beyond-lease each have a green asserting test. |
| HDR:L67 | API keys and provider credentials are never printed, committed, or stored in plaintext artifacts. | open | standing-invariant | Enforce with (1) pre-commit/CI secret scan over the repo and docs/baselines artifacts, (2) run-log review that batch runners print key NAMES not values, (3) continue the disposable-key practice for burst providers. | script (secret scan) + junior-agent hygiene | repo-wide; docs/baselines/*.json; batch runner logs; RunPod/provider configs | CONT | A leaked provider key in a committed baseline artifact is unrevokable history. | feasible — Consistent with existing practice (disposable keys for goal-mode ops per user memory). Baseline artifacts inspected this session contain no credentials. | Secret-scan pass (zero findings) on the checklist-era commits and docs/baselines/; scan wired so a finding fails the run. |
| HDR:L69 | Embeddings and optional LLMs may generate candidates or enrich fields, but only deterministic evidence-backed policy may grant final document seats. | open | standing-invariant | Enforce the seat-granting boundary in review + tests: only deterministic evidence-backed policy (reservation_policy / ranking_policy / shelf_engine rules) grants final document seats; embeddings/LLM outputs may only produce candidates or enrich fields. Concretely: S8 must wire shelf_reserve THROUGH reservation_policy — a second independent seat path is banned. | junior-agent (S8 implementer + reviewers) | services/retriever/reservation_policy.py, ranking_policy, services/librarian/shelf_engine; grounded_planner (candidates only) | CONT | Deterministic seats are what make selection auditable and eval-comparable; LLM-granted seats are unreproducible. | feasible — TERMINOLOGY (registry_leftover 'document seats'): 'seat' spans two granularities — evidence seat (reservation/ranking_policy) vs document seat (librarian shelf_engine). No literal Seat class exists; grep 'seat' in docstrings/diagnostics. The duplicate-seat-system risk is explicitly flagged; S8's design already bans it ('never the old unconditional floor'). | Seat diagnostics show every seated doc with a deterministic reason code; asserting test that disabling the LLM changes NO seat grants (only enrichment/explanations). |
| HDR:L72 | An LLM may explain an already-seated path but may not add, remove, or protect books in the final selection. | open | standing-invariant | Enforce explain-not-select as a code-boundary test: the LLM explanation stage (Phase 4 'explain an already-seated path') runs strictly after final selection and its output cannot mutate the seated set; assert selection hash identical before/after the explain call. | junior-agent (Phase-4 implementer + test) | librarian Phase 4 (LLM amplification) seam; final_selector / seat diagnostics | CONT | Keeps the trust boundary of L69 airtight at the last stage where an LLM touches results. | feasible — Phase 4 is 0/8 built (critique table), so today this is vacuously true; the invariant matters when Phase-4 work lands (post-S8). The 'explain-not-select' box is one of the 8 Phase-4 items. | Asserting test: seated-set hash pre/post explanation identical; explanation stored as annotation, never as selection input. |
| HDR:L77 | `polymath_v2`: 84,987 summary points; 67,953 have explicit empty `summary_model`. | na | superseded | Re-check the number: it matches docs/baselines/BASELINE_2026-07-13.json (summary_integrity.polymath_v2: 84,987 / 67,953, captured 02:52Z @d3159b8) — but it is SUPERSEDED as current state by the same-day P0.1 green verify (86,880 points, 0 empty-model). Relabel the ledger section 'pre-P0.1 census baseline (frozen)' or restamp with post-P0.1 counts per Standing Rule 3. | junior-agent (ledger restamp) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:77-78; docs/baselines/BASELINE_2026-07-13.json | LOG | A 'Current Durable Baseline' that contradicts the current durable state misleads every agent that reads it before dispatch. | already-done — Numbers verified against the census artifact this session (exact match). Staleness verified against PLAN_CRITIQUE line 19/70 (V receipts). This is the exact case Standing Rule 3 was written for. | Ledger edit relabeling the section as frozen pre-P0.1 census (pointer to BASELINE_2026-07-13.json) OR restamped counts from a fresh capture_raptor_baseline.py run, dated. |
| HDR:L79 | `ecommerce_AI_FILM_SCHOOL`: 9,375 summary points; 5,796 have explicit empty `summary_model`. | na | superseded | Re-check the number: matches BASELINE_2026-07-13.json (ecommerce_AI_FILM_SCHOOL: 9,375 / 5,796) — superseded as current state by P0.1 verify (9,453 points, 0 empty-model). Restamp with L77. | junior-agent (ledger restamp) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:79-80; docs/baselines/BASELINE_2026-07-13.json | LOG | Same staleness as L77; ecom is a PoC-pair corpus so agents read this row when planning S4. | already-done — Exact match to census artifact verified this session; contradicted-by-later-verify per critique line 19. | Same single ledger edit as L77 (one restamp covers lines 77-84). |
| HDR:L81 | `markbuildsbrands_transcripts`: 657 summary points; 12 have explicit empty `summary_model`. | na | superseded | Re-check the number: matches BASELINE_2026-07-13.json (markbuildsbrands_transcripts: 657 / 12) — superseded as current state by P0.1 verify (1,009 points, 0 empty-model; count GREW post-census, so this row is doubly stale). Restamp with L77. | junior-agent (ledger restamp) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:81-82; docs/baselines/BASELINE_2026-07-13.json | LOG | mark is the S10/S13 pilot corpus; its summary census will be read repeatedly. | already-done — Exact match to census artifact verified; post-P0.1 count 1,009 vs census 657 shows a regeneration pass ran after capture. | Same single ledger edit as L77. |
| HDR:L83 | `UGO_CORPUS`: 202 summary points; all 202 have explicit empty `summary_model`. | na | superseded | Re-check the number: matches BASELINE_2026-07-13.json (UGO_CORPUS: 202 / 202 empty) — superseded as current state by P0.1 verify (203 points, 0 empty-model). Restamp with L77. | junior-agent (ledger restamp) | docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:83-84; docs/baselines/BASELINE_2026-07-13.json | LOG | UGO is the canary; an all-placeholder census reading would wrongly disqualify it as the S1/S4 canary if trusted. | already-done — Exact match verified; 202->203 fully-attributed per critique line 19. UGO's census also shows lexicon_points=0 and tier0_doc_cards=0 — worth carrying into the S6 readiness split as a metadata-quality datum. | Same single ledger edit as L77. |
| HDR:L85 | Summary jobs: 50,522 succeeded, 500 queued, 775 superseded. | na | done-verified | Re-check the number: matches BASELINE_2026-07-13.json (summary_jobs_by_status: succeeded 50,522 / queued 500 / superseded 775). Re-probe current queue depth before S4 (the 500 queued were pre-P0.1-regen; the queue state has certainly moved): `docker exec <backend> python -c` Mongo count on summary_jobs by status. | script (Mongo count probe) | Mongo summary_jobs collection; docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:85 | LOG | Stale queue counts mislead S4 batch sizing and pressure-gate expectations. | already-done — Census match verified this session. As a CURRENT claim it is done-unverified (post-census P0.1 regen ran 2,633 jobs); cheap probe closes it. | Fresh summary_jobs by-status counts with date, pasted into the restamped baseline section. |
| HDR:L86 | Summary tree: 21,432 sections, 20,942 one-child sections, 23,394 rollups. | na | done-verified | Re-check the number: matches BASELINE_2026-07-13.json (21,432 sections / 20,942 one-child / 23,394 rollups). Note for interpretation: 'one-child sections' = tree nodes with a single child_node_id (passthrough sections, P0.2's target), i.e. 98% of sections are passthroughs — the P0.2 open items live on exactly these rows. Re-probe after any tree rebuild. | script (tree census probe) | summary_tree store (Qdrant 'schemas' collection family / Mongo tree docs); docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:86 | LOG | The one-child ratio is the load-bearing motivation for P0.2's passthrough handling and the omit-vs-alias owner decision. | already-done — Census match verified. TERMINOLOGY (registry_hierarchy): 'one-child section' counts child_node_ids (tree ids), NOT parent_ids (member parent-chunk ids) — auditors re-deriving this number must use the right id space. 'Summary tree' is canonical (summary_tree); it is NOT RAPTOR clustering (that is P3.1). | Tree census re-run matching or restamping the three counts, using child_node_ids for the one-child predicate. |
| HDR:L87 | `authentic_library`: deleted/quarantined; historical Qdrant collections still exist. | na | done-unverified | Re-verify both halves: (1) corpus status — BASELINE_2026-07-13.json shows authentic_library status=deleted, cleanup_status=running (verified); (2) 'historical Qdrant collections still exist' — probe Qdrant `GET /collections` for corpus_f8a0aa85* / authentic_library-era collections and reconcile against ORPHAN_MANIFEST_2026-07-13.json; deletion of leftovers is P0.6 gap-filler work (plan line 95). | script (Qdrant collections probe) + junior-agent (P0.6 orphan reconcile) | Qdrant collections; docs/baselines/ORPHAN_MANIFEST_2026-07-13.json; docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:87-88 | LOG | Orphaned collections are dead weight and a cross-corpus leakage hazard if any query path enumerates collections. | feasible — First half verified against the census artifact this session (cleanup was 'running' at capture — terminal state unconfirmed). Second half is unverified since capture; the orphan manifest exists as the reconcile target. | Qdrant collection list diffed against ORPHAN_MANIFEST_2026-07-13.json with each historical collection either justified (backup/quarantine) or scheduled for P0.6 purge under lease rules. |
| HDR:L89 | Grounded planner: enabled flag set, durable budget 500, model unset; therefore not operational. | na | informational | Re-check the fact via Mongo settings probe (grounded planner enabled flag, durable budget, model field); the FIX is P1.2's open activation work: set provider/model + budget honestly, but activation stays BLOCKED until card.v0 + seats + eval baseline exist (post-S8 per P1.2's own activation-block item). Do not 'fix' by just setting a model now. | script (settings probe); junior-agent (P1.2, post-S8); owner (model/budget choice) | Mongo settings (grounded planner config); backend grounded_planner.py; docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:89-90 | LOG | An enabled-but-modelless planner is a silent no-op — the worst kind of dark config for eval interpretation. | feasible — Claim internally consistent (enabled+model-unset => not operational) and matches P1.2's critique-table open items ('provider/model+budget'). TERMINOLOGY (registry_retrieval 'grounded planner'): bare 'planner' is ambiguous — the deterministic query planner (query_plan.py, QueryPlanV2) is a DIFFERENT component and IS operational; this line concerns only grounded_planner. Also note the critique's 'positional 6-match cap still open' is STALE — fix is in code (vocabulary.py:1705, grounded_planner.py:375 per registry_alias 'strong match'). | Settings probe output showing flag/budget/model triplet, dated; P1.2 activation later closes with its own acceptance receipts. |
| HDR:L91 | Recent deployed latency probes: Fast 39.8s degraded, Hybrid 14.0s, Graph 18.9s, cross-corpus Hybrid 23.3s. | na | build | Re-check the numbers against docs/baselines/LATENCY_2026-07-13.json (captured 06:38Z — BEFORE S0's warmup+cache landed at 327da45), then re-run the latency probe post-S0 as the S0 row itself promises ('refreshed latency probe'): probe_tier_latency across the 4 run shapes and file LATENCY_<date>.json; investigate whether Fast 39.8s 'degraded' was cold-start (trace shows 26s inside Local RAG retrieval) now that embedder warmup is live. | script (probe_tier_latency) run by junior-agent | docs/baselines/LATENCY_2026-07-13.json (+ new artifact); :8082 embedder sidecar; backend /api/chat SSE probe | S0 | Pre-warmup latency numbers understate the deployed system and would corrupt every later A/B latency comparison. | feasible — Artifact verified present and matching the claim's shape; the capture predates S0's live warmup+cache receipts, so the 'degraded' Fast figure is plausibly stale. S0 is otherwise closed — this refreshed probe is its one outstanding receipt (no post-S0 LATENCY_*.json exists in docs/baselines/). | New LATENCY_*.json captured post-327da45 deployment, with Fast/Hybrid/Graph/cross-corpus figures; ledger line restamped citing it. |
| HDR:L93 | Reranker readiness includes real startup inference. Embedder `/health` currently verifies model load and stall state only; it does not certify a completed inference w… | na | superseded | Restamp this line per Standing Rule 3 — the embedder half is STALE: wave1/warm merged (7f8e7b2) and S0 receipt 327da45 shows live :8082/health with tri-state liveness/model_loaded/inference_ready + warmup{complete:true, duration_s:0.045, vector_dim:1024, model}. Re-verify with `curl :8082/health` and update the sentence; for the reranker half, confirm WHICH binary serves :8081 (deployed = torch fp16 per plist; reranker_mlx is rollback; the repo /reranker docker service has NO warmup) before trusting 'includes real startup inference'. | junior-agent (ledger restamp + curl probes) | scripts/apple_ml_services/embedder_mlx/main.py; :8082/health; :8081 reranker sidecar; docs/RAPTOR_RAG_IMPLEMENTATION_CHECKLIST.md:93-95 | S0 | Readiness semantics gate every latency/eval interpretation; a ledger that understates warmup coverage invites redundant fixes. | already-done — Embedder warmup verified in code this session (WARMUP_TEXTS, tri-state readiness, embedder_mlx/main.py:11,49-51,134-137) and in the S0 commit's live-health receipt. TERMINOLOGY (registry_leftover): THREE things answer to 'the reranker/embedder' — repo docker services (no warmup), MLX sidecars (warmup), and for rerank the deployed torch-fp16 :8081; readiness claims must name the deployed binary. This checklist sentence predates the merge and is stale by Standing Rule 3. | curl :8082/health showing warmup.complete=true + :8081 identity/readiness output pasted into the restamped ledger line, dated. |
