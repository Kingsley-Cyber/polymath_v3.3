RESUME: The owner's first action = host relief (Docker restart/reboot;
see 08:41Z receipt). Then execute its POST-RELIEF QUEUE: fresh baseline
triplet -> librarian re-window (W1c gates, real latency) -> W1d (+v3) ->
W1e (fast route) -> re-acceptance. All quality work is DONE and receipted;
only host-dependent latency verdicts remain. Skip anything receipted.
